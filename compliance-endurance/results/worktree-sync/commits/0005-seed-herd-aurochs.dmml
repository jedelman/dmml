machine herd/aurochs
  states
    grazing
    migrating
    culled
    thriving

  transition migrate(destination)
    grazing -> migrating
    guard self `grazesIn` forest/oldroot
    assert migrating
    retract grazing

  transition settle(pasture)
    migrating -> grazing
    guard self `migratesTo` $pasture
    assert grazing
    retract migrating

  transition cull(hunter)
    grazing -> culled
    guard $hunter `worksAt` self
    assert culled
    retract grazing

  transition recover()
    culled -> thriving
    assert thriving
    retract culled
