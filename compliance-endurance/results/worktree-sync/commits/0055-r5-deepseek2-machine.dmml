machine herd/aurochs

  states
    grazing
    migrating
    culled
    thriving

  transition cull(hunter)
    grazing -> culled
    guard $hunter `worksAt` self
    assert culled
    retract grazing

  transition recover()
    culled -> thriving
    assert thriving
    retract culled