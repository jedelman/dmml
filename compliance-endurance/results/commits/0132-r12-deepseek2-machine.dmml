machine cistern/ninefathom
  states
    ruptured
    sealed
    flowing

  transition seal(sealer)
    ruptured -> sealed
    guard $sealer `seals` self
    assert sealed

  transition open()
    sealed -> flowing
    assert flowing
    retract sealed