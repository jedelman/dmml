machine smithy/anvil

  states
    idle
    forged

  transition forge()
    idle -> forged
    guard ore/raw1 `refinedInto` ingot/batch1
    assert ingot/batch1 `title` "A forged ingot"
    assert forged
    retract idle
