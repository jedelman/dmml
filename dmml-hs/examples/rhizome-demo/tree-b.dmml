machine room/b
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard room/a `cleared` mark/yes
    assert self `cleared` mark/yes
    assert self `state` open
    retract self `state`

