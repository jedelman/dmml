machine room/c
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard room/b `cleared` mark/yes
    assert self `cleared` mark/yes
    assert self `state` open
    retract self `state`

