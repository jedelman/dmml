machine room/wVault
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard room/wForge `cleared` mark/yes
    guard vault/keyring `has` key/gold
    assert self `cleared` mark/yes
    assert self `yields` sigil/wVault
    assert self `state` open
    retract self `state`

