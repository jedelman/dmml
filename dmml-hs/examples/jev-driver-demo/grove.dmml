machine world/grove

  states
    idle

  transition plantOak(spot)
    idle -> idle
    spawn $spot from flora/oakTemplate
    assert $spot `state` seed

  transition plantPine(spot)
    idle -> idle
    spawn $spot from flora/pineTemplate
    assert $spot `state` seed
