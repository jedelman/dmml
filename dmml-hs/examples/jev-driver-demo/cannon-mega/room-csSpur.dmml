machine room/csSpur
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard path/cs `cleared` mark/yes
    assert self `yields` relic/csSpur
    assert self `state` open
    retract self `state`

