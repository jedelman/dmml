machine room/cHall2
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard room/cHall `cleared` mark/yes
    assert self `cleared` mark/yes
    assert self `state` open
    retract self `state`

