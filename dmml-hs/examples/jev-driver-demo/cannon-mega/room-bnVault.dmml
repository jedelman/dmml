machine room/bnVault
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard path/bn `cleared` mark/yes
    guard vault/keyring `has` key/gold
    assert self `cleared` mark/yes
    assert self `yields` sigil/bnVault
    assert self `state` open
    retract self `state`

