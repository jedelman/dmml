commit setup
  declare relation state
  declare relation avail
  declare relation need

  field/patchA `state` lush
  field/patchA `avail` status/lush
  herd/deer1 `state` hungry
  herd/deer2 `state` hungry
  herd/fawn `need` status/hungry
  world/season `state` active
