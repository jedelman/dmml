machine room/bsHall
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard path/bs `cleared` mark/yes
    assert self `cleared` mark/yes
    assert self `state` open
    retract self `state`

