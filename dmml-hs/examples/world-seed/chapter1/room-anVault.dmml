machine room/anVault
  states
    sealed
    open

  transition breach()
    sealed -> open
    guard room/anForge `cleared` mark/yes
    guard vault/keyring `has` key/gold
    assert self `cleared` mark/yes
    assert self `yields` sigil/anVault
    assert self `state` open
    retract self `state`

