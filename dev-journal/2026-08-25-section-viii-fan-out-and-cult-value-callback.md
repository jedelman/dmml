# Section VIII: a fan-out, mirroring Section VII's fan-in (2026-08-25)

`dmml/examples/benjamin_section_viii.rs` models Section VIII's explicit
"twofold consequence" of camera-mediated (rather than in-person)
presentation: the performance itself fragments into "a series of optical
tests" (production side — cameraman repositions, editor assembles the
sequence), and separately the audience loses personal contact and starts
identifying with the camera rather than the actor (reception side).
Modeled as a real fan-out — both consequence commits consume only the
root mediation fact, and checked directly: neither cites the other. This
is the mirror image of Section VII's fan-in (many independent witnesses
converging on one shared diagnosis) — here, one cause branches into two
independent effects.

Both consequences then converge, together with an explicit callback to
Sections V–VI's cult-value material (re-declared minimally, per this
series' standing convention), on the paragraph's closing verdict: "this
is not the approach to which cult values may be exposed." Modeled as a
convergence commit with consumes count 3, checked to confirm the closing
sentence is really drawing on all three threads rather than restating
the audience-posture consequence alone.

Applied, not re-discovered: Section VI's cite-and-spend finding. The root
mediation fact's persistence is checked by materializing it ALONE, not by
querying its key inside the combined log (which would read back `None`
once both consequences have cited it — the same retraction behavior,
now used correctly rather than tripped over).
